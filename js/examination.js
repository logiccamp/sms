
for (let i = 0; i <= 60; i++){
    document.getElementById('ca_list').innerHTML += `<option value='${i}'>${i}</option>`
}

for (let i = 0; i <= 100; i++){
    document.getElementById('exam_list').innerHTML += `<option value='${i}'>${i}</option>`
}